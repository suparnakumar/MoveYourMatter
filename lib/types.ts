// Shared domain types used across the app

export type Rasa = {
  slug: string;
  name: string;
  theme: string;
  colour: string;
};

export type Video = {
  url: string;
  title: string;
  duration_seconds: number;
};
